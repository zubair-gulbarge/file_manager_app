# file_manager_app
Develop a Python-based web application that implements a full CRUD (Create, Read, Update, Delete) lifecycle for user accounts and a file-sharing system. The system must distinguish between standard users and an administrative account.
